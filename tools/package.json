{
  "name": "@extensions-demo/tools",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "stripe:import-products": "ts-node stripe-import-products.ts",
    "firebase:deploy-firestore-rules": "firebase deploy --only firestore:rules",
    "firebase:start-emulators": "firebase emulators:start --only auth,firestore"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^10.0.0",
    "firebase-admin": "^9.11.1",
    "stripe": "^8.172.0",
    "ts-node": "^10.2.1"
  }
}
